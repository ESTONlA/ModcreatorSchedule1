<UserControl x:Class="Schedule1ModdingTool.Views.NpcSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:Schedule1ModdingTool.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="30" d:DesignWidth="300">
    <ComboBox x:Name="NpcComboBox"
              SelectedValue="{Binding SelectedNpcId, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
              SelectedValuePath="Id"
              FontSize="10"
              IsEditable="True">
        <ComboBox.ItemTemplate>
            <DataTemplate DataType="{x:Type viewModels:NpcInfo}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" Margin="0,0,8,0" />
                    <TextBlock Text="(" Foreground="{StaticResource MediumTextBrush}" />
                    <TextBlock Text="{Binding Id}" Foreground="{StaticResource MediumTextBrush}" />
                    <TextBlock Text=")" Foreground="{StaticResource MediumTextBrush}" />
                    <TextBlock Text=" - " Foreground="{StaticResource MediumTextBrush}" Margin="4,0,4,0" />
                    <TextBlock Foreground="{StaticResource MediumTextBrush}" FontSize="9">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="Base Game" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsModNpc}" Value="True">
                                        <Setter Property="Text" Value="Mod" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </DataTemplate>
        </ComboBox.ItemTemplate>
    </ComboBox>
</UserControl>

