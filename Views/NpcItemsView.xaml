<UserControl x:Class="Schedule1ModdingTool.Views.NpcItemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ContextMenu x:Key="NpcContextMenu">
            <MenuItem Header="Add NPC" Click="AddNpc_Click">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="{StaticResource PlusIcon}"
                                              Foreground="{Binding RelativeSource={RelativeSource AncestorType=MenuItem}, Path=Foreground}"
                                              Width="16" Height="16"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Remove NPC" Click="RemoveNpc_Click">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="{StaticResource DeleteIcon}"
                                              Foreground="{Binding RelativeSource={RelativeSource AncestorType=MenuItem}, Path=Foreground}"
                                              Width="16" Height="16"/>
                </MenuItem.Icon>
            </MenuItem>
        </ContextMenu>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource DarkPanelBrush}"
                BorderBrush="{StaticResource DarkBorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="16,8">
            <StackPanel Orientation="Horizontal">
                <Button Content="← Back to Categories"
                        Click="BackToCategories_Click"
                        Style="{StaticResource IconButtonStyle}"
                        Foreground="{StaticResource AccentOrangeBrush}"
                        Padding="8,4"
                        Cursor="Hand"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Auto"
                      Background="{StaticResource DarkBackgroundBrush}">
            <Grid Margin="16" ContextMenu="{StaticResource NpcContextMenu}">
                <ItemsControl ItemsSource="{Binding CurrentProject.Npcs}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CategoryTileStyle}"
                                    Width="220" Height="150"
                                    Margin="8"
                                    MouseLeftButtonDown="NpcTile_MouseLeftButtonDown"
                                    MouseRightButtonDown="NpcTile_MouseRightButtonDown"
                                    Cursor="Hand">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <materialDesign:PackIcon Grid.Row="0"
                                                             Kind="{StaticResource NPCsIcon}"
                                                             Foreground="{StaticResource MediumTextBrush}"
                                                             Width="48" Height="48"
                                                             HorizontalAlignment="Center"
                                                             Margin="0,0,0,8"/>

                                    <StackPanel Grid.Row="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding DisplayName}"
                                                   FontSize="16"
                                                   FontWeight="SemiBold"
                                                   Foreground="{StaticResource LightTextBrush}"
                                                   HorizontalAlignment="Center"
                                                   TextAlignment="Center"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding NpcId}"
                                                   FontSize="11"
                                                   Foreground="{StaticResource DimTextBrush}"
                                                   HorizontalAlignment="Center"/>
                                    </StackPanel>

                                    <Border Grid.Row="2"
                                            Background="{StaticResource DarkBorderBrush}"
                                            CornerRadius="4"
                                            Padding="8,4"
                                            Margin="8"
                                            HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding ClassName}"
                                                   FontSize="10"
                                                   FontWeight="SemiBold"
                                                   Foreground="{StaticResource MediumTextBrush}"
                                                   TextTrimming="CharacterEllipsis"
                                                   Width="180"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <StackPanel HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="{Binding CurrentProject.Npcs.Count, Converter={StaticResource CountToVisibilityConverter}}">
                    <TextBlock Text="🧑"
                               FontSize="64"
                               HorizontalAlignment="Center"
                               Opacity="0.3"
                               Margin="0,0,0,16"/>
                    <TextBlock Text="No NPCs in your project"
                               HorizontalAlignment="Center"
                               FontStyle="Italic"
                               Foreground="{StaticResource DimTextBrush}"
                               FontSize="14"/>
                    <TextBlock Text="Right-click and choose Add NPC to create one"
                               HorizontalAlignment="Center"
                               FontSize="11"
                               Foreground="{StaticResource DimTextBrush}"
                               Margin="0,8,0,0"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
